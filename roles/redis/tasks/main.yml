- name: install redis server
  apt:
    name: redis-server
    state: present
    update_cache: yes

- name: update redis config
  replace:
    path: /etc/redis/redis.conf
    regexp: '127.0.0.1'
    replace: '0.0.0.0'

- name: start redis
  systemd:
    name: redis
    state: restarted
    enabled: yes